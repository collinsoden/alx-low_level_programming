#include <stdio.h>
/**
 * main - prints all single numbers base 10
 * Return: Always (0) Success
 **/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf(num);
	}
	printf('\n');
	return (0);
}
